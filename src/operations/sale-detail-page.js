export function saleDetailPage(saleId) {
  const safeId = JSON.stringify(String(saleId || ""));
  return `<!doctype html>
<html lang="es">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Expediente · MARTCOM Next</title>
<style>
:root{font-family:Inter,system-ui,-apple-system,sans-serif;color:#172033;background:#f4f6fa}*{box-sizing:border-box}body{margin:0}.top{background:#101827;color:#fff;padding:18px 24px}.top a{color:#c7d2fe;text-decoration:none}.wrap{max-width:1320px;margin:auto;padding:22px}.hero{display:flex;justify-content:space-between;gap:18px;align-items:flex-start;margin-bottom:18px}.hero h1{margin:4px 0 6px;font-size:26px}.muted{color:#687386}.pill{display:inline-flex;padding:5px 9px;border-radius:999px;background:#eef2ff;font-size:12px;font-weight:650}.ok{background:#e8f8ef;color:#17663a}.bad{background:#fff0f0;color:#a62f2f}.warn{background:#fff7df;color:#8a5b00}.layout{display:grid;grid-template-columns:minmax(0,2fr) minmax(340px,1fr);gap:16px}.panel{background:#fff;border:1px solid #e1e6ef;border-radius:14px;padding:17px;box-shadow:0 3px 14px #1118270a;margin-bottom:16px}.panel h2{font-size:16px;margin:0 0 14px}.data{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.field{padding:10px;background:#f8fafc;border-radius:9px}.field small{display:block;color:#778195;margin-bottom:3px}.reqs{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.req{padding:11px;border:1px solid #e6e9ef;border-radius:10px}.req b{display:block;margin-bottom:3px}.docs{display:grid;gap:8px}.doc{display:flex;justify-content:space-between;align-items:center;gap:8px;padding:10px;border:1px solid #e6e9ef;border-radius:10px}.doc a{color:#3346a8}.timeline{display:grid;gap:10px}.event{position:relative;padding:12px 12px 12px 18px;border:1px solid #e5eaf1;border-radius:11px;background:#fafbfd;overflow:hidden}.event:before{content:'';position:absolute;width:4px;left:0;top:0;bottom:0;background:#64748b}.event-head{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.event-title{font-size:13px;font-weight:700;color:#263348}.event-date{font-size:11px;color:#778195;white-space:nowrap}.event-summary{font-size:12px;color:#566176;margin-top:7px;line-height:1.45;overflow-wrap:anywhere}.event-details{margin-top:8px}.event-details summary{cursor:pointer;font-size:11px;color:#64748b}.event-details pre{white-space:pre-wrap;overflow-wrap:anywhere;word-break:break-word;font-size:10px;line-height:1.4;background:#f1f4f8;border-radius:8px;padding:8px;margin:7px 0 0;max-height:180px;overflow:auto}.actions{display:flex;gap:8px;flex-wrap:wrap}.btn{border:0;border-radius:9px;padding:9px 12px;background:#1f2937;color:#fff;cursor:pointer;text-decoration:none;font-size:13px}.btn.secondary{background:#e7eaf0;color:#172033}.btn.danger{background:#a52f2f}.btn:disabled{opacity:.45;cursor:not-allowed}.progress{display:grid;grid-template-columns:repeat(5,1fr);gap:5px;margin:12px 0}.step{height:8px;border-radius:99px;background:#e5e7eb}.step.done{background:#334155}.missing{margin-top:10px;padding:10px;background:#fff7df;border-radius:9px;color:#815500;font-size:13px}.paymentbox{border:1px solid #dce5ef;border-radius:12px;padding:14px;background:#fbfdff}.paymenthead{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:12px}.paymentgrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.paymentfield{padding:10px;background:#f6f8fb;border-radius:9px}.paymentfield small{display:block;color:#778195;margin-bottom:3px}.prooflink{display:inline-block;margin-top:10px;color:#3346a8}.empty{padding:18px;color:#778195;text-align:center}.error{padding:20px;background:#fff0f0;color:#9b2c2c;border-radius:12px}@media(max-width:850px){.layout{grid-template-columns:1fr}.data,.reqs,.paymentgrid{grid-template-columns:1fr}.hero{flex-direction:column}.progress{grid-template-columns:repeat(5,1fr)}}
</style></head>
<body>
<div class="top"><a href="/operations">← Mesa de Operaciones</a></div>
<div class="wrap"><div id="app" class="empty">Cargando expediente…</div></div>
<script>
const saleId=${safeId};
const token=new URLSearchParams(location.search).get('token')||localStorage.getItem('nextOpsToken')||'';if(token)localStorage.setItem('nextOpsToken',token);
const headers={'content-type':'application/json','x-operations-token':token};
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function money(v){return v==null?'—':new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN',maximumFractionDigits:0}).format(v)}
function date(v){if(!v)return '—';try{return new Intl.DateTimeFormat('es-MX',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v))}catch{return v}}
function statusLabel(v){return String(v||'').replaceAll('_',' ')}
function progress(s){const order=['capture','validation','validity','payment','completed'];const q=s.queue||'capture';const idx=order.indexOf(q);return order.map((x,i)=>'<div class="step '+(i<=idx?'done':'')+'" title="'+x+'"></div>').join('')}
const eventNames={'sale.created':'Expediente creado','capture.started':'Captura iniciada','capture.completed':'Alta procesada','documents.synced':'Documentos actualizados','validation.check.updated':'Validación actualizada','validation.approved':'Validación aprobada','validation.rejected':'Validación rechazada','validation.correction.requested':'Corrección solicitada','validation.reopened':'Validación reabierta','validity.confirmed':'Vigencia confirmada','validity.document.delivered':'Vigencia enviada al cliente','validity.issue':'Incidencia de vigencia','validity.reopened':'Vigencia reabierta','payment.requested':'Pago solicitado','payment.accounts_image.delivered':'Cuentas enviadas al cliente','payment.received':'Comprobante de pago recibido','payment.validated':'Pago validado · expediente cerrado','payment.issue':'Incidencia de pago','payment.reopened':'Pago reabierto'};
function eventSummary(e){const d=e.details||{};switch(e.type){case 'documents.synced':return d.complete?'Documentación completa · '+esc(d.file_count??0)+' archivo(s)':'Documentación actualizada';case 'payment.requested':return 'Monto: '+money(d.amount)+(d.due_date?' · Fecha límite: '+esc(d.due_date):'');case 'payment.accounts_image.delivered':return 'Imagen de cuentas enviada al cliente'+(d.accounts_image_name?' · '+esc(d.accounts_image_name):'');case 'payment.received':return 'Registrado por '+esc(d.by||'Mia')+(d.proof_name?' · '+esc(d.proof_name):'');case 'payment.validated':return 'Validado por '+esc(d.by||'Cobranza');case 'validity.confirmed':return 'Vigencia confirmada'+(d.document_name?' · '+esc(d.document_name):'');case 'validity.document.delivered':return 'Documento de vigencia enviado al cliente'+(d.document_name?' · '+esc(d.document_name):'');case 'validation.approved':return 'Revisión final aprobada'+(d.by?' por '+esc(d.by):'');case 'capture.completed':return 'Alta procesada y enviada a Validación';default:return d.reason?esc(d.reason):d.notes?esc(d.notes):''}}
function paymentPanel(s){const p=s.payment||{};if(!p.requested&&!p.received&&!p.validated)return '';const state=p.validated?'Validado':p.received?'Recibido · pendiente de validación':p.requested?'Solicitado':'Pendiente';const stateClass=p.validated?'ok':p.received?'warn':'';const proof=p.proof_url?'<a class="prooflink" target="_blank" rel="noopener" href="'+esc(p.proof_url)+'">Abrir comprobante</a>':(p.proof_name?'<div class="muted" style="margin-top:10px">El comprobante quedó registrado, pero no tiene URL disponible.</div>':'');return '<section class="panel"><h2>Pago / Comprobante</h2><div class="paymentbox"><div class="paymenthead"><b>Estado del pago</b><span class="pill '+stateClass+'">'+esc(state)+'</span></div><div class="paymentgrid"><div class="paymentfield"><small>Monto solicitado</small>'+money(p.amount??s.sale?.precio)+'</div><div class="paymentfield"><small>Fecha de recepción</small>'+date(p.received_at)+'</div><div class="paymentfield"><small>Registrado por</small>'+esc(p.received_by||'—')+'</div><div class="paymentfield"><small>Comprobante</small>'+esc(p.proof_name||'Pendiente')+'</div>'+(p.reference?'<div class="paymentfield"><small>Referencia</small>'+esc(p.reference)+'</div>':'')+(p.validated_at?'<div class="paymentfield"><small>Validado</small>'+date(p.validated_at)+'</div>':'')+'</div>'+proof+(p.received&&!p.validated?'<div class="missing"><b>Pendiente:</b> Cobranza debe validar el comprobante antes de cerrar el expediente.</div>':'')+'</div></section>'}
async function api(url,method='GET',body){const sep=url.includes('?')?'&':'?';const authedUrl=url+sep+'token='+encodeURIComponent(token);const r=await fetch(authedUrl,{method,headers,body:body?JSON.stringify(body):undefined});const j=await r.json();if(!r.ok)throw new Error(j.error||'Error');return j}
async function action(url,body={}){try{await api(url,'POST',body);await load()}catch(e){alert(e.message)}}
function render(s,links){
 const checklist=s.documents?.checklist?.requirements||[];const files=s.documents?.files||[];const complete=Boolean(s.documents?.complete);const events=[...(s.events||[])].reverse();
 const requirements=checklist.map(r=>'<div class="req"><b>'+(r.received?'✓ ':'✕ ')+esc(r.label)+'</b><span class="pill '+(r.received?'ok':'bad')+'">'+(r.received?'Recibido':'Pendiente')+'</span></div>').join('');
 const documents=files.length?files.map(f=>'<div class="doc"><div><b>'+esc(f.name||'Archivo')+'</b><div class="muted">'+esc(f.type||'other')+' · '+date(f.received_at)+'</div></div>'+(f.url?'<a target="_blank" rel="noopener" href="'+esc(f.url)+'">Abrir</a>':'<span class="muted">Sin URL</span>')+'</div>').join(''):'<div class="empty">Todavía no hay archivos asociados.</div>';
 const timeline=events.length?events.map(e=>{const summary=eventSummary(e);return '<div class="event"><div class="event-head"><span class="event-title">'+esc(eventNames[e.type]||statusLabel(e.type))+'</span><span class="event-date">'+date(e.at)+'</span></div>'+(summary?'<div class="event-summary">'+summary+'</div>':'')+'<details class="event-details"><summary>Ver detalle técnico</summary><pre>'+esc(JSON.stringify(e.details||{},null,2))+'</pre></details></div>'}).join(''):'<div class="empty">Sin eventos.</div>';
 let actions='';
 if(s.queue==='capture'){actions+='<button type="button" class="btn" data-action="capture-start" data-sale-id="'+esc(s.sale_id)+'">Tomar expediente</button>';actions+='<button type="button" class="btn" '+(!complete?'disabled':'')+' data-action="capture-complete" data-sale-id="'+esc(s.sale_id)+'">Alta procesada</button>';actions+='<button type="button" class="btn secondary" data-action="documents-sync" data-sale-id="'+esc(s.sale_id)+'">Recalcular documentos</button>'}
 if(s.queue==='validation')actions+='<a class="btn" href="/operations?queue=validation&token='+encodeURIComponent(token)+'">Ir a Validación</a>';
 if(s.queue==='payment'&&s.status==='payment_received'){actions+='<button type="button" class="btn" data-action="payment-validate" data-sale-id="'+esc(s.sale_id)+'">Validar pago y cerrar</button>';actions+='<button type="button" class="btn danger" data-action="payment-issue" data-sale-id="'+esc(s.sale_id)+'">Reportar incidencia</button>'}
 if(s.queue==='payment'&&s.status==='payment_issue')actions+='<button type="button" class="btn" data-action="payment-reopen" data-sale-id="'+esc(s.sale_id)+'">Reabrir pago</button>';
 if(links?.chatwoot)actions+='<a class="btn secondary" target="_blank" rel="noopener" href="'+esc(links.chatwoot)+'">Ver conversación</a>';
 document.getElementById('app').innerHTML='<div class="hero"><div><span class="pill">'+esc(s.sale_id)+'</span><h1>'+esc(s.customer?.nombre||'Cliente sin nombre')+'</h1><div class="muted">Chat #'+esc(s.conversation_id||'—')+' · '+esc(s.sale?.plan||'Sin plan')+' · '+money(s.sale?.precio)+'</div><div class="progress">'+progress(s)+'</div></div><span class="pill '+(complete?'ok':'warn')+'">'+(complete?'Expediente completo':'Expediente incompleto')+'</span></div><div class="layout"><main><section class="panel"><h2>Datos del cliente</h2><div class="data"><div class="field"><small>CURP</small>'+esc(s.customer?.curp||'Pendiente')+'</div><div class="field"><small>NSS</small>'+esc(s.customer?.nss||'Pendiente')+'</div><div class="field"><small>Edad</small>'+esc(s.customer?.edad??'—')+'</div><div class="field"><small>Actividad</small>'+esc(s.customer?.actividad||'—')+'</div><div class="field"><small>Teléfono</small>'+esc(s.customer?.telefono||'—')+'</div><div class="field"><small>Estado</small>'+esc(statusLabel(s.status))+'</div></div></section><section class="panel"><h2>Checklist documental</h2><div class="reqs">'+requirements+'</div>'+(!complete?'<div class="missing"><b>Faltantes:</b> '+esc((s.documents?.missing||[]).join(', ')||'requisitos pendientes')+'</div>':'')+'</section><section class="panel"><h2>Documentos recibidos</h2><div class="docs">'+documents+'</div></section>'+paymentPanel(s)+'</main><aside><section class="panel"><h2>Acciones</h2><div class="actions">'+actions+'</div><div class="muted" style="margin-top:12px">Responsable: '+esc(s.capture?.assigned_name||'Sin asignar')+'<br>Inicio: '+date(s.capture?.started_at)+'<br>Actualizado: '+date(s.updated_at)+'</div></section><section class="panel"><h2>Historial del expediente</h2><div class="timeline">'+timeline+'</div></section></aside></div>'
}
async function load(){try{const j=await api('/operations/api/sales/'+encodeURIComponent(saleId));render(j.sale,j.links||{})}catch(e){document.getElementById('app').innerHTML='<div class="error">'+esc(e.message)+'</div>'}}
document.getElementById('app').addEventListener('click',e=>{const button=e.target.closest('[data-action]');if(!button)return;const id=button.dataset.saleId;const actionName=button.dataset.action;if(actionName==='capture-start')action('/operations/api/sales/'+encodeURIComponent(id)+'/capture/start',{name:'Capturista'});else if(actionName==='capture-complete')action('/operations/api/sales/'+encodeURIComponent(id)+'/capture/complete',{});else if(actionName==='documents-sync')action('/operations/api/sales/'+encodeURIComponent(id)+'/documents/sync',{});else if(actionName==='payment-validate'){if(confirm('¿Confirmas que Cobranza validó este comprobante y deseas cerrar el expediente?'))action('/operations/api/sales/'+encodeURIComponent(id)+'/payment/validate',{by:'Cobranza'});}else if(actionName==='payment-issue'){const reason=prompt('Describe la incidencia del pago:');if(reason)action('/operations/api/sales/'+encodeURIComponent(id)+'/payment/issue',{reason,by:'Cobranza'});}else if(actionName==='payment-reopen')action('/operations/api/sales/'+encodeURIComponent(id)+'/payment/reopen',{by:'Cobranza'});});
const es=new EventSource('/operations/api/events?token='+encodeURIComponent(token));es.onmessage=e=>{try{const x=JSON.parse(e.data);if(x.sale?.sale_id===saleId)load()}catch{}};load();
</script></body></html>`;
}
